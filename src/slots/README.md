# Nude Slots

- Convenient data structure that allows easy access to named slots
- Use manual slot assignment without giving up named slot assignment!
- Auto-slot by CSS selector (Use `data-assign` on the slot element)
