# ElementInternals plugin

- Symbol to access element internals
- Automatically create `ElementInternals` lazily when accessed
- No errors thrown for subsequent calls, just returns the existing `ElementInternals` object
